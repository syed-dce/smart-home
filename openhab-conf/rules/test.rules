rule "Sock1MotionOn"
when
Item Pir changed to 1
then
sendCommand(Sock1, ON)
postUpdate(Sock1, ON)
logInfo("org.openhab","Pir -> ON")
end

rule "Sock1MotionOff"
when
Item Pir changed to 0
then
sendCommand(Sock1, OFF)
logInfo("org.openhab","Pir -> OFF")
end
